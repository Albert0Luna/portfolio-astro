---
interface Props {
  text: string;
  textEs: string;
  href: string;
}

const { text, textEs, href } = Astro.props;

const pathname = Astro.url.pathname;
const lang = pathname.split("/")[1] || "en"; // fallback to 'en'
---

<section id={href} class="py-24">
  <h2
    class="mb-12 font-serif text-3xl font-bold tracking-tight text-slate-900 dark:text-white md:text-4xl"
  >
    {lang === "es" ? textEs : text}
  </h2>
  <slot />
</section>
